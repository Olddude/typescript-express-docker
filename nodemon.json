{
  "watch": [
    "packages/server-base/src",
    "packages/authorization-server/src",
    "packages/resource-server/src"
  ],
  "ext": "ts",
  "ignore": ["**/*.spec.ts"],
  "exec": "yarn install --frozen-lockfile && yarn setup && yarn build && yarn down && yarn up",
  "inspect": true,
  "verbose": true
}
